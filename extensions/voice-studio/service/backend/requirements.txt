# CMR-007: `vieneu` is pinned because the SDK version decides which checkpoint `Vieneu()`
# loads by default (3.2.x -> VieNeu-TTS v3 Turbo, Apache-2.0). An unpinned SDK could silently
# move to a checkpoint with a different licence lineage. See
# docs/compliance/tts-model-license-evidence.md before bumping this.
vieneu==3.2.3
fastapi>=0.110
uvicorn[standard]>=0.29
