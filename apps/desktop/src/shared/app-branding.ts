export const APP_NAME = 'Izzi AI';

// Keep the legacy ID stable so existing installs, user data, notifications,
// and auto-update metadata continue to resolve to the same Windows app.
export const APP_ID = 'com.izziapi.openclaw';
