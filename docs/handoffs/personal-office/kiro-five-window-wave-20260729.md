# Kiro five-window wave — 2026-07-29

These prompts preserve the five-lane model while keeping at most two source
writers. Kiro must stop on provider-credit exhaustion and return a compact
handoff; Codex then resumes from committed evidence.

## W0 — Control mirror, read-only

Work only from `F:\Ai Tools\_wt-starizzi-personal-office-baseline`.

Read `integration-ledger.json`, `leases.json`, acceptance records and active
producer handoffs. Do not edit, stage or commit anything. Never touch
quarantine. Audit that Loop 06 and Loop 10 remain the only writers, report
lease drift, dependency drift, forbidden paths or secret risk. W0 authority
remains Codex; return recommendations only.

Use Socrates + orchestrator and:
`/search-first /context-gatherer /quick-spec /security-review /verification-loop /understand-codebase`.

## W1 — Independent reviewer, read-only

Use the accepted integration ref as evidence. Prepare independent security and
contract review checklists for Loop 06 and Loop 10. Do not write producer
worktrees or coordinator files. When a producer reaches READY_FOR_REVIEW,
review exact commits for scope, secrets, tenant/workspace binding, tamper
resistance and tests. Return PASS or SEND-BACK with file/line evidence.

Use Socrates + orchestrator and the full required skill list from the active
dispatch.

## W2 — Loop 12 preparation, read-only

Study Loop 02 shell, Loop 03 work engine, Customer Marketing contracts and
`legacy-personal-office-mapping.md` sections assigned to Loop 12. Produce a
quick-spec and exact-path lease request only; do not edit source, App.tsx,
Sidebar.tsx, DB, IPC or Customer Marketing files. Identify the smallest
adapter-only first slice and dependency gates on Loops 06, 10 and 11.

Use Socrates + orchestrator + builder in planning-only mode and the full
required skill list. Design skills must define the eventual integration
quality bar, not generate UI yet.

## W3 — Loop 06 shadow review, read-only

Codex builder is the only Loop 06 writer. Read
`dispatch-loop-06.md`, the active lease and the current Loop 06 worktree.
Do not edit, stage or commit. Challenge route determinism, URL parsing,
same-route retry, secret redaction, official-host headers and preservation of
Loop 05 host/context bytes. Return findings to W0; do not start Loop 11 until
Loop 06 is ACCEPTED and W0 grants a new lease.

Use Socrates + orchestrator and the full required skill list.

## W4 — Loop 10 producer

Work only in `F:\Ai Tools\_wt-starizzi-personal-office-loop10` on
`feature/personal-office-loop-10-20260729`.

Read `dispatch-loop-10.md` and `LEASE-L10-INTEGRATION-GRANTS-20260729`
completely before editing. You are the only Loop 10 writer. Implement exactly
the leased contract/mapping/grant-vault scope using two-phase commits. No raw
secrets, network, OAuth, shell, persistence, DB/schema, IPC, renderer,
manifests, production integration service, quarantine, push or deploy. Submit
READY_FOR_REVIEW, never ACCEPTED.

Use Socrates + orchestrator + builder and:
`/search-first /context-gatherer /quick-spec /backend-patterns /frontend-patterns /deployment-patterns /security-review /verification-loop /understand-codebase Design /gpt-taste /design-taste-frontend /stitch-design-taste`.

## Credit exhaustion handoff

If Kiro reports quota/credit/provider failure:

1. Stop before another edit.
2. Record branch, HEAD, status, changed paths, tests and blocker.
3. Commit only if the current phase is coherent and inside lease; otherwise
   leave exact diff and report it.
4. Do not switch worktrees or ask another Kiro window to write the same paths.
5. Return control to Codex, which will preserve/review and continue.
